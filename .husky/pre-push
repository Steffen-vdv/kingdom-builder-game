#!/usr/bin/env sh
# Pre-push hook - runs quality checks before push
# Linting is handled by pre-commit hook; this does typecheck + dependency validation

set -e

# Run typecheck (always full - it's fast due to incremental builds)
npm run typecheck

# Run dependency cruiser to validate package boundaries
npm run lint:deps
