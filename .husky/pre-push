#!/usr/bin/env sh
# Pre-push hook - runs quick quality checks before pushing
# Full verification available via `npm run verify`

set -e

# Run parallel quality checks (format, typecheck, lint)
npm run check:parallel
