#!/usr/bin/env sh

# Exit on errors
set -e

# Run lint-staged on changed files (runs Prettier, ESLint, and a global type check)
npx lint-staged

# Run the fast Vitest suite without redundant lint/type checks
npm run test:quick

