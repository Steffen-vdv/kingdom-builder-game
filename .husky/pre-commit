#!/usr/bin/env sh

# Exit on errors
set -e

# Run lint-staged on changed files
npx lint-staged

# Run the full typecheck + lint suite before tests so config regressions fail fast
npm run check

# Run the fast Vitest suite without redundant lint/type checks
npm run test:quick

