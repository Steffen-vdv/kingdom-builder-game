#!/usr/bin/env sh
# Pre-commit hook - formats all files and adds changes to the commit

set -e

# Format all files
npm run format

# Stage any formatting changes to tracked files (not untracked)
git add -u
