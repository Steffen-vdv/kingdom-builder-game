#!/usr/bin/env sh

# Exit on errors
set -e

# Run lint-staged on changed files
npx lint-staged

# Run the lighter CI check pipeline before tests to avoid redundant executions
npm run check:ci

# Run the fast Vitest suite without redundant lint/type checks
npm run test:quick

